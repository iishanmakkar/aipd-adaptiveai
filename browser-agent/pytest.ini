[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    live: needs the full compose stack and spends real NIM quota (deselected by default)
