# Browser-agent service env (copy to .env; never commit - gitignored).
# REQUIRED for VLM page descriptions; /browse + /fill work without it.
NIM_API_KEY=
NIM_BASE_URL=https://integrate.api.nvidia.com/v1
NIM_MODEL=meta/llama-3.2-11b-vision-instruct
PORT=8003
HOST=0.0.0.0
