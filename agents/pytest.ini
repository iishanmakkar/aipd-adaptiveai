[pytest]
testpaths = tests
python_files = test_*.py
addopts = -m "not live" -q
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    live: needs the real embedding model download and/or NIM quota (deselected by default)
