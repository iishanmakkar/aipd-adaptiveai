[pytest]
testpaths = tests
python_files = test_*.py
addopts = -m "not live" -q
asyncio_default_fixture_loop_scope = function
markers =
    live: needs a running service and/or spends real NIM quota (deselected by default)
