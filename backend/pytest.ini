[pytest]
testpaths = tests
python_files = test_*.py
addopts = -m "not live" -q
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    live: needs a reachable Postgres (and/or real services) - deselected by default
